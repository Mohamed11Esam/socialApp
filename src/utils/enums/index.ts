

export enum ROLE{
    USER = "user",
    ADMIN = "admin",
    SUPER_ADMIN = "super_admin"
}
export enum GENDER {
    MALE = "male",
    FEMALE = "female"
}

export enum USER_AGENT {
    LOCAL = "local",
    GOOGLE = "google"
}
